Module Module1

    Sub Main()

        Dim a As String
        Dim b As Integer
        a = "978"
        b = CInt(a)
        Console.WriteLine(b)
        Console.ReadKey()
        'Dim comma, character As Char
        'Dim index, count, number, total As Integer
        'Dim strnum, Instring As String
        'Dim avg As Decimal

        'Console.WriteLine("Enter your series of whole numbers(separated by commas)")
        'Instring = Console.ReadLine

        'comma = ","
        'For index = 1 To Len(Instring)
        'character = Mid(Instring, index, 1)
        'If character <> comma Then
        'strnum = strnum & character
        'Else
        'number = CInt(strnum)
        'total = total + number
        'count = count + 1
        'End If


        'Next

        'avg = total / count
        'Console.WriteLine("Total value= " & total)
        'Console.Write("Average value= " & avg)

        'Console.ReadKey()


    End Sub

End Module
